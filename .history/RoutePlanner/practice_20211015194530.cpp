#include <fstream>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>

using std::cout;
using std::endl;
using std::string;
using std::vector;

void testing(int option) {
  switch (option) {
  case 0:
    // Test to write out to terminal;
  break;
  case 1:
    // Test to read a file
    break;
  case 2:
    // Test to read a line
    break;
  case 3:

      // Test to parse a line
      break;
  }
}

int main() { testing(0); }